pipeline {
    agent any
    parameters {
            booleanParam(name: 'MODIFICATION', defaultValue: false, description: 'If you want to add something')
        }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
         }
        stage('Copy') {
            steps {
                 sh '''#!/bin/bash
                 mkdir -p home/app
                 cp -r ./api/target/api.jar ./home/app/
                 cp -r ./integration/target/integration.jar ./home/app/
                 '''
            }
        }

        stage('Deploy') {
            when {
                expression {params.MODIFICATION}
            }
            steps {
                sh '''#!/bin/bash
                java -jar -Dspring.profiles.active=prod ./home/app/api.jar  &
                java -jar -Dspring.profiles.active=prod ./home/app/integration.jar &
                '''
            }
        }
    }
}
